// base http client axios instance
import axios, { AxiosInstance } from "axios";

class BaseAPIClient {
    // this class will contains all the interception methods of the axios to refresh an token and call all other api with HTTP methods
}
export default new BaseAPIClient();
